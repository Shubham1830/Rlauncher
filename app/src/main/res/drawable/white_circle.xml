<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer circle (black circle) -->
    <item android:drawable="@drawable/black_circle" />

    <!-- Inner circle (white circle) -->
    <item
        android:bottom="55dp"
        android:left="55dp"
        android:right="55dp"
        android:top="55dp">
        <shape
            android:shape="oval">
            <solid android:color="#F9F7F7" />
        </shape>
    </item>
</layer-list>